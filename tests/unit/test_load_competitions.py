# from tests.conftest import data_mock
#
# def test_load_competitions(loaded_competitions, data_mock):
#     """
#       Test that the load_competitions() function correctly reads competition data
#       from a mocked JSON file and returns a list of competitions
#       with 'name', 'date', and 'numberOfPlaces' keys.
#     """
#     assert loaded_competitions == data_mock['competitions']
